import React from "react";
import { ProfileImageContainer } from "../styled/styled";

const Profile = () => {
  return (
    <div>
      <ProfileImageContainer></ProfileImageContainer>
    </div>
  );
};

export default Profile;
